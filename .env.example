# OpenAI API key. Used server-side only by the Vite dev proxy and the
# Vercel serverless function in /api/openai/*. Never exposed to the client.
#
# Local dev: copy this file to `.env.local` and fill in the key.
# Vercel: add OPENAI_API_KEY as a project environment variable.
OPENAI_API_KEY=sk-...
